# Redis Configuration for Final Golf SaaS Development
# Optimized for development environment with persistence

# Basic Configuration
port 6379
bind 0.0.0.0
protected-mode no
timeout 300
tcp-keepalive 300

# Memory Management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence Configuration
# Save if at least 1 key changed in 900 seconds
save 900 1
# Save if at least 10 keys changed in 300 seconds
save 300 10
# Save if at least 10000 keys changed in 60 seconds
save 60 10000

# AOF Configuration for better durability
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# Logging
loglevel notice
logfile ""

# Performance Tuning
tcp-backlog 511
databases 16

# Security (for development)
# requirepass your_password_here  # Uncomment and set password for production

# Slow Log Configuration
slowlog-log-slower-than 10000
slowlog-max-len 128

# Client Configuration
maxclients 10000

# Memory Usage Reporting
# latency-monitor-threshold 100

# Pub/Sub Configuration (for real-time features)
client-output-buffer-limit pubsub 32mb 8mb 60

# Performance Optimizations
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
list-max-ziplist-size -2
list-compress-depth 0
set-max-intset-entries 512
zset-max-ziplist-entries 128
zset-max-ziplist-value 64

# Disable dangerous commands in development
# rename-command FLUSHDB ""
# rename-command FLUSHALL ""
# rename-command CONFIG ""